---
const { items = [] } = Astro.props;
---
<ol class="breadcrumb">
  <li><a href="/">Home</a></li>
  {items.map((item) => <li>{item.href ? <a href={item.href}>{item.label}</a> : item.label}</li>)}
</ol>
